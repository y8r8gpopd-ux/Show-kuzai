<div class="ingredients-all">
  <% @ingredients.each do |ingredient| %>
    <div class="ingredient-item">
      <%= link_to ingredient.name, edit_ingredient_path(ingredient.id) %>
    </div>
  <% end %> 
</div>
